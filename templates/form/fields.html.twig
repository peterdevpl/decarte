{% use "bootstrap_3_layout.html.twig" %}

{# Widgets #}

{% block image_file_widget %}
    {% if url %}<p><img src="{{ asset(url) }}" width="200"></p>{% endif %}
    {{ block('form_widget_compound') }}
{% endblock image_file_widget %}
