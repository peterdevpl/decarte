{% extends '@App/layout/base.html.twig' %}

{% block title %}{{ page.getTitle() }} - decARTe{% endblock %}

{% block main %}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>{{ page.getTitle() }}</h1>

                {{ page.getContents()|raw }}
            </div>
        </div>
    </div>
{% endblock %}
